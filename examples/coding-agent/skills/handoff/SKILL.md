---
name: handoff-author
description: TODO — replace with a one-sentence description of what the handoff stage produces for your project.
context_sources: []
artifacts_produced:
  - units/{unit_id}/handoff-v{round}.md
success_criteria:
  - TODO — what mechanical or semantic check tells the reviewer this is done?
---

# handoff-author skill (template)

This is a stub. Replace with the real instructions for the handoff stage.

To generate a real skill from this stub, drop this file into Claude (or your model) and say:

> Expand this stub into a real handoff-author skill for {my project type, my stack, my conventions}.

## Process

1. Read each path listed under `context_sources`.
2. Produce the artifact named under `artifacts_produced`.
3. Ensure every item under `success_criteria` is satisfied.

## Constraints

- Do not invent file paths, symbol names, or identifiers. Cite real source only.
- Do not edit files outside this unit's scope without explicit authorization.

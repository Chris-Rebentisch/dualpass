# Backlog — {{project_name}}

Work-unit registry. One row per unit. Status flips at handoff close.

## Status legend

- `[ ]` open — not yet started
- `[~]` in progress — at least one stage has launched
- `[!]` blocked — see `.dualpass-state/<unit>-stuck-*.md` for details
- `[x]` shipped — handoff closed, FINAL artifacts ratified

## Units

| Status | Unit ID | Headline | Owner | Phase | Notes |
|---|---|---|---|---|---|
| `[ ]` | demo-001 | Example unit — replace this row | architect | discovery | scaffolded by `dualpass init` |
